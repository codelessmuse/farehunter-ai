// Tailwind is processed via @tailwindcss/vite in vite.config.ts (Tailwind v4).
// Keep this file for tooling that expects PostCSS; add plugins here if needed later.
export default {
  plugins: {},
};
